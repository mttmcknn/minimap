# Python To Rust Migration Plan

This migration plan is complete and superseded by the lean v1 Rust design.

The active implementation no longer preserves the old Python-era command model.
Use [docs/MINIMAP_V1_LEAN_DESIGN.md](docs/MINIMAP_V1_LEAN_DESIGN.md) for the
current schema, command surface, and validation strategy.
